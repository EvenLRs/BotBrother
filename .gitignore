# ── 敏感配置（含密钥，绝不入库）──
config.json
config.json.bak
config.json.tmp
webui.json
# WebUI 认证数据（密码哈希）
data/

# ── 运行产物 / 日志 ──
*.log
*.log.old
*.log.*

# ── 开发过程文档（会话工作产物，非项目交付物）──
BLOCKED.md
PROGRESS.md

# ── Python ──
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
/test/
/test
# 测试套件仅限本地开发，不入库（历史已重写移除）
/tests/
/tests

# ── 编辑器 / 系统 ──
.DS_Store
.idea/
.vscode/
*.swp

# ── Agent 规则文件（本地专属，不入库）──
/AGENTS.md

# ── macOS launchd 资产（本地保留，发布不含；install.sh 的 launchd 分支已按需手动提供）──
/macos/

# ── 浏览器验证截图 / 临时产物 ──
*.png
!docs/**/*.png
